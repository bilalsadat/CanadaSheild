{
  "expo": {
    "name": "KinShield",
    "slug": "kinshield",
    "scheme": "kinshield",
    "version": "1.0.0",
    "orientation": "portrait",
    "userInterfaceStyle": "dark",
    "backgroundColor": "#0B0F1A",
    "newArchEnabled": true,
    "splash": {
      "backgroundColor": "#0B0F1A",
      "resizeMode": "contain"
    },
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.kinshield.app",
      "config": { "usesNonExemptEncryption": false }
    },
    "android": {
      "package": "com.kinshield.app",
      "edgeToEdgeEnabled": true
    },
    "plugins": [
      "expo-router",
      ["expo-camera", { "cameraPermission": "KinShield uses the camera only to scan QR codes you point at, so it can check them for fraud before you open them." }]
    ],
    "experiments": { "typedRoutes": false }
  }
}
